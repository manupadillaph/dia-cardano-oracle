node_modules/
dist/
.env
.env.*
!.env.example
*.sqlite
*.sqlite-journal
# Runtime state — generated while the feeder runs; not committed.
# Bootstrap artifacts (state/*/config-bootstrap.json, state/*/clients/*.json)
# are committed so a fresh clone is ready to run without a manual copy step.
state/*/logs/
state/*/feeder-checkpoint.json
state/*/clients/*/pairs/
